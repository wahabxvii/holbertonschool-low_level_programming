#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *@v: character in ASCII
 * Return: absolute value
 */
int _abs(int v)
{
if (v >= 0)
{
return (v);
}
else
{
return (v * -1);
}
}
