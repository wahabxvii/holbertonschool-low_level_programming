#include "main.h"
/**
 * print_sign - prints the sign of the number
 * Return: 1 if +, 0 if 0 or -1 if -
 */
int print_sign(int n)
{
if (n > 0)
{
_putchar('+'','' ''\n', n);
}
else if (n = 0)
{
_putchar('0'','' ''\n', n);
}
else
{
_putchar('-'','' ''\n', n)
}
_putchar('\n')
}
