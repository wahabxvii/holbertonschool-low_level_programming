#include "main.h"
/**
 * isdigit - checks for a digit (0 through 9)
 * @c: an ASCII character
 * Return: 1 if c is a character or 0 if not
 */
int _isdigit(int c)
{
	int a = 1 , b = 0;
	if (c >= 0 && c <= 9)
	{
		return (a);
	}
	else
	{
		return (b);
	}
}
